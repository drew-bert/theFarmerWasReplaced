# General Notes

#plant block
#carrots
#till block
#if (get_ground_type() == GroundsGrassland)till()
#if (get_ground_type() == GroundsSoil and get_entity_type() !=Entities.Carrot):
plant(Entities.Carrot)
#move block